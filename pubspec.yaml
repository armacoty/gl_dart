name: gl_dart
version: 1.0.0+1
description: dart:ffi bindings for OpenGL library. It connects the opengl32.dll/libgl.so library and loads the functions of the specified version of opengl.
homepage: https://github.com/armacoty/gl_dart

environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  ffi: ^1.1.2
